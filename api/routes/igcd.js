class IGreatestCommonDivisor {
    /**
     * 
     * @param {array} numbers array of numbers to be computed
     * @returns the greatest common divisor of all numbers
     */
    greatestCommonDivisor(numbers) {
        return numbers;
    };
}

module.exports = greatestCommonDivisor;